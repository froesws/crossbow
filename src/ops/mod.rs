//! Operations module - business logic for Series and DataFrame operations
//! Organized by responsibility (SRP): each submodule has a specific purpose
//!
//! Note: During refactoring phase, operation modules are being created as placeholders
//! while the legacy Series and DataFrame operations remain in their original files.
//! These modules will be gradually populated as code is reorganized.

// pub mod series_math;        // Element-wise arithmetic operations
// pub mod series_agg;         // Aggregation operations
// pub mod series_utils;       // Utility operations
pub mod dataframe_filter;   // DataFrame filtering operations (placeholder)
pub mod dataframe_sort;     // DataFrame sorting operations (placeholder)
pub mod dataframe_groupby;  // DataFrame group-by operations (placeholder)
